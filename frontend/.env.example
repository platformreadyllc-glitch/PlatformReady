# Backend API URL. Required when running the frontend from a device on the LAN.
# If unset, the browser falls back to http://localhost:3000 which only works
# when the browser and backend are on the same machine.
VITE_API_URL=http://<backend-machine-ip>:3000
